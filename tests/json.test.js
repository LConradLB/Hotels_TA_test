describe("JSON Tests",()=>{
    describe("PARSING TO",()=>{
        it("Should parse an object to JSON",()=>{
            expect(x).to.eql(y)
        })
    })

    describe("PARSING FROM",()=>{
        it("Should parse from JSON to an object",()=>{
            expect(x).to.eql(y)
        })
    })

})